// PNGs opcionais: se o arquivo existir em game/assets/, ele substitui a
// textura procedural de mesmo nome. Enquanto não existirem, o jogo usa
// os desenhos procedurais (textures.js).
export const OPTIONAL_PNG_ASSETS = Object.freeze([
  // { key: "player", file: "player/player.png" },
  // { key: "egg", file: "props/egg.png" }
]);
